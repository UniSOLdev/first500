/**
 * Protected app layout — Lead integrator wires AppShell + CoachProvider after specialist agents complete.
 * Placeholder passes children through until integration.
 */
export default function AppLayout({ children }: LayoutProps<"/">) {
  return <>{children}</>;
}
